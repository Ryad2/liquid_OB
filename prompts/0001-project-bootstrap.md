# 0001: Project bootstrap

## User request

Initialize `Ryad2/liquid_OB` cleanly before implementing the hackathon project.
The architecture and code must be written from scratch, with no reuse from the
previous repository.

## Implementation constraints

- Do not add protocol equations or business logic in the bootstrap commit.
- Establish reproducible Foundry and TypeScript toolchains.
- Make secret boundaries explicit.
- Add CI and local quality commands before feature work.
- Preserve an auditable, incremental Git history.
- Record AI assistance and all external dependencies.
