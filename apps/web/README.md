# Web application

React and TypeScript interface built with Vite. The current page is a bootstrap
health check, not the product design.

Only variables prefixed with `VITE_` are exposed to browser code. Sponsor API
keys and signing material must remain in a server-side service.

```bash
pnpm --filter @liquid-ob/web dev
```
